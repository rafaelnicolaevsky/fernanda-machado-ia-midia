# fernanda-machado-ia-midia

Repositório de mídia pública do perfil de IA Fernanda Machado — hospeda as
imagens dos posts pra consumo via `raw.githubusercontent.com` pela
Instagram Graph API (que exige `image_url` público).

Gerenciado automaticamente por `scripts/utils/hospedagem_midia.py` no
projeto `fernanda_machado_ia`. Não editar manualmente.
